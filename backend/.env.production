# Production Environment Variables
# Copy this to your App Runner or Elastic Beanstalk environment

# Database (RDS PostgreSQL)
PGHOST=your-rds-endpoint.us-east-1.rds.amazonaws.com
PGPORT=5432
PGDATABASE=spaw_db
PGUSER=postgres
PGPASSWORD=your_strong_password

# Node Environment
NODE_ENV=production
PORT=8080

# CORS - Add your frontend URL(s) separated by commas
CORS_ORIGIN=https://your-frontend-url.com,https://dxxxxx.cloudfront.net

# Redis (optional, if you use sessions)
REDIS_URL=redis://your-redis-url:6379

# Other configs
JWT_SECRET=your_jwt_secret_key
